## Access Pattern Demo
Simple Python prototype demonstrating three behavioral design patterns:
- **Singleton** (`AccessPolicyManager`)
- **Factory Method** (`UserFactory`)
- **Observer** (`DashboardDisplay`, `LogService`)

Run:
```bash
python patterns_demo.py
